#include "main.h"
/**
 * @i: to increase 
 * print_times_table - print time table 
 * @n: to increase n 
 **/
void print_times_table(int n)
{
	if (!((n < 0) || (n > 15)))
	{
		int i = 0;
		while (i < n)
		{
			_putchar(i);
			
		}
	}
}
